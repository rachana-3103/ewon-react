import { FC } from "react";
import { StyledLogo } from "./style";

const Logo: FC = () => {
    return (
        <StyledLogo>
            do<span>ar</span>
        </StyledLogo>
    );
};

export default Logo;
