import { FC } from "react";
import { Card } from "@doar/components";
import { StyledHeader } from "./style";

const Acquisition: FC = () => {
    return (
        <Card>
            <StyledHeader>Acquisition</StyledHeader>
        </Card>
    );
};

export default Acquisition;
