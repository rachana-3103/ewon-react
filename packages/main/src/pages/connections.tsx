import React from "react";
import Layout from "../layouts";
import Content from "../layouts/content";

const Connections: React.FC = () => {
    return (
        <Layout>
            <Content>dsfsd</Content>
        </Layout>
    );
};

export default Connections;
